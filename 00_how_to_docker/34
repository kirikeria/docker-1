# 34. Delete the Aiur virtual machine without using rm -rf.

docker-machine rm -y Aiur

# docker-machine rm --help: -y — Assumes automatic yes to proceed with remove, without prompting further user confirmation.
